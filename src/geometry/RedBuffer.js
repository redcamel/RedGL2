"use strict";
var RedBuffer;
(function () {
    RedBuffer = function (key, data) {
        
    }
    RedBuffer.ARRAY_BUFFER = 'arrayBuffer';
    RedBuffer.ELEMENT_ARRAY_BUFFER = 'elementArrayBuffer';
    Object.freeze(RedBuffer);
})()